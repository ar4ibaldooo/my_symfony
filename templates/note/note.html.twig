{% extends 'base.html.twig' %}
{% block body %}
    {% for flashMessage in app.session.flashbag.get('notice') %}

        <div class="alert alert-success">
            {{ flashMessage }}
            {{ form_start(MassageType) }}
            {{ form_end(MassageType) }}
        </div>
        <br>

    {% endfor %}
    {{ form_start(NoteType) }}
    {{ form_row(NoteType.email) }}
    {{ form_row(NoteType.images) }}
    {{ form_end(NoteType) }}
{% endblock %}